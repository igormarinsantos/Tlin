---
phase: 06-sistema-editorial-seo-geo-e-llm
plan: "04"
type: execute
wave: 4
depends_on: [06-03]
files_modified:
  - app/blog/page.tsx
  - components/blog/ArticleCard.tsx
  - components/blog/BlogSearchAndGrid.tsx
  - components/blog/FeaturedCarousel.tsx
  - components/blog/categoryVisuals.ts
  - tests/editorial/migration-parity.test.ts
files_deleted:
  - lib/blog.ts
autonomous: true
requirements: [BLOG-01, BLOG-07]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-10/D-11: listagem, cards, carrossel, busca, navegação, compartilhamento e aparência essencial continuam funcionando para os três slugs."
    - "D-12: nenhum consumidor importa `lib/blog.ts`; o arquivo monolítico só é removido depois de todos os consumidores migrarem."
    - "A listagem pública recebe somente summaries da consulta publicada e não expõe rascunho ou data futura no HTML nem no cliente."
  artifacts:
    - path: app/blog/page.tsx
      provides: "Hub principal SSR alimentado por getPublishedArticles"
    - path: tests/editorial/migration-parity.test.ts
      provides: "Regressão completa após remoção do legado"
  key_links:
    - from: app/blog/page.tsx
      to: lib/editorial/queries.ts
      via: "consulta summaries publicados antes de entregar busca/carrossel"
      pattern: "getPublishedArticles"
    - from: components/blog/ArticleCard.tsx
      to: lib/editorial/types.ts
      via: "props usam EditorialArticleSummary"
      pattern: "EditorialArticleSummary"
---

<objective>
Concluir a migração visível do blog e remover o monólito somente depois que todos os consumidores estiverem no registro canônico.

Purpose: fechar BLOG-07 sem manter duas fontes concorrentes.
Output: home do blog e componentes migrados, `lib/blog.ts` excluído e paridade integral verde.
</objective>

<execution_context>
@C:/Users/Igor Marin Santos/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Igor Marin Santos/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-sistema-editorial-seo-geo-e-llm/06-CONTEXT.md
@.planning/phases/06-sistema-editorial-seo-geo-e-llm/06-PATTERNS.md
@.planning/phases/06-sistema-editorial-seo-geo-e-llm/06-03-SUMMARY.md
@docs/design-system.md
@docs/quality/visual-baseline.md

<interfaces>
- `getPublishedArticles()` retorna artigos validados/ordenados; derivar `EditorialArticleSummary` antes de atravessar a fronteira de client component.
- `ArticleCard`, `BlogSearchAndGrid` e `FeaturedCarousel` mantêm seus contratos visuais, mas trocam `BlogArticle` por `EditorialArticleSummary`.
- `formatArticleDate` e `getChatGptSummaryUrl` devem ser movidos para utilitários editoriais ou substituídos por exports equivalentes, sem recriar o array.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrar home, busca, cards e carrossel para summaries publicados</name>
  <files>app/blog/page.tsx, components/blog/ArticleCard.tsx, components/blog/BlogSearchAndGrid.tsx, components/blog/FeaturedCarousel.tsx, tests/editorial/migration-parity.test.ts</files>
  <read_first>app/blog/page.tsx, components/blog/ArticleCard.tsx, components/blog/BlogSearchAndGrid.tsx, components/blog/FeaturedCarousel.tsx, components/blog/categoryVisuals.ts, lib/editorial/types.ts, lib/editorial/queries.ts, lib/blog.ts, tests/editorial/fixtures/legacy-articles.ts, tests/editorial/migration-parity.test.ts, docs/design-system.md, docs/quality/visual-baseline.md</read_first>
  <behavior>
    - A home renderiza os três artigos publicados via SSR antes da busca client-side.
    - Busca por title/description/tema continua funcional e nunca recebe artigos não publicados.
    - Carrossel, cards, links “Ler artigo” e “Resumir com IA” preservam destinos, foco e layout essencial.
  </behavior>
  <action>Alterar `app/blog/page.tsx` para buscar `getPublishedArticles()` no servidor e projetar somente o summary serializável necessário aos componentes. Atualizar tipos/imports dos componentes, preservando classes, ordem visual, gradientes, responsividade, foco e links atuais per D-11. O filtro client-side permanece aprimoramento, não fonte de arquitetura; todo artigo e tema importante deve continuar presente em links HTML SSR. Estender o teste de paridade para listagem, carrossel, busca, compartilhamento e navegação dos três slugs.</action>
  <acceptance_criteria>
    - A home não importa registry cru, módulos de artigo nem o array legado.
    - Client components recebem apenas artigos já filtrados pela query publicada.
    - Os três slugs continuam navegáveis e o visual essencial segue a baseline em desktop/mobile.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- tests/editorial/migration-parity.test.ts</automated>
    <fails_when>o comando sai diferente de zero ou listagem, links, compartilhamento, busca ou estrutura visual falham para qualquer slug</fails_when>
    <automated>npm run typecheck</automated>
    <fails_when>o comando sai diferente de zero ou um componente ainda depende do tipo BlogArticle legado</fails_when>
  </verify>
  <done>A superfície visível do blog usa summaries publicados sem regressão funcional.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Remover o array legado após provar ausência de consumidores</name>
  <reversibility rating="costly">A exclusão encerra a compatibilidade interna; reverter exige restaurar o monólito e reintroduzir imports que a fase removeu.</reversibility>
  <files>lib/blog.ts, components/blog/categoryVisuals.ts, tests/editorial/migration-parity.test.ts</files>
  <read_first>lib/blog.ts, components/blog/categoryVisuals.ts, lib/editorial/types.ts, lib/editorial/registry.ts, lib/editorial/queries.ts, tests/editorial/fixtures/legacy-articles.ts, tests/editorial/migration-parity.test.ts, app/blog/page.tsx, app/blog/[slug]/page.tsx, app/blog/rss.xml/route.ts, app/sitemap.ts</read_first>
  <behavior>
    - Não existe import rastreado de `@/lib/blog` ou acesso a `BLOG_ARTICLES` depois da exclusão.
    - Os três slugs/canonicals continuam em params, listagem, sitemap e RSS sem redirect.
    - A fixture histórica permanece independente do código de produção.
  </behavior>
  <action>Antes de excluir `lib/blog.ts`, fazer `categoryVisuals` resolver cores/ícones pelos IDs tipados de taxonomia e usar busca estática para migrar todo import restante para tipos, queries ou utilitários editoriais já criados. Excluir o arquivo somente quando os testes comprovarem os três módulos no registro e zero consumidor. Não copiar o array para outro módulo nem manter fallback silencioso. Reexecutar a matriz integral de BLOG-07 e typecheck.</action>
  <acceptance_criteria>
    - `lib/blog.ts` não existe e seu conteúdo não reaparece em outro array monolítico.
    - Não há import de produção para o caminho removido nem referência a `BLOG_ARTICLES`.
    - Visuais de categoria resolvem por IDs tipados do registro, sem duplicar taxonomia em strings livres.
    - Os três slugs/canonicals permanecem idênticos e resolvem sem redirect.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- tests/editorial/migration-parity.test.ts tests/editorial/content-contract.test.ts</automated>
    <fails_when>o comando sai diferente de zero ou qualquer slug/superfície falha após a remoção</fails_when>
    <automated>npm run typecheck</automated>
    <fails_when>o comando sai diferente de zero por import legado ou incompatibilidade de tipo</fails_when>
  </verify>
  <done>Há uma única fonte editorial por arquivo e nenhum consumidor legado.</done>
</task>

</tasks>

## Artifacts this phase produces

- `app/blog/page.tsx` — home SSR alimentada por `getPublishedArticles`.
- Componentes `ArticleCard`, `BlogSearchAndGrid` e `FeaturedCarousel` tipados por `EditorialArticleSummary`.
- Remoção definitiva de `lib/blog.ts` após gate de paridade.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| server component → client components | Summaries editoriais serializados entram na busca e no carrossel do navegador |

## STRIDE Threat Register — ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-06-04 | Information Disclosure | home/busca | high | mitigate | filtrar no servidor pela query canônica antes de entregar props ao cliente; regressão cobre draft/future |
| T-06-06 | Denial of Service | busca/carrossel | low | accept | acervo local pequeno e já compilado; manter filtro em memória sem nova entrada remota |
</threat_model>

<verification>
- `npm test -- tests/editorial/migration-parity.test.ts tests/editorial/content-contract.test.ts`
- `npm run typecheck`
- `git diff --check`
</verification>

<success_criteria>
- BLOG-07 cobre URL, canonical, sitemap, RSS, compartilhamento, navegação e aparência essencial dos três artigos.
- O monólito é removido somente depois da migração integral.
- Nenhum rascunho ou publicação futura alcança componentes client-side.
</success_criteria>

<output>
Create `.planning/phases/06-sistema-editorial-seo-geo-e-llm/06-04-SUMMARY.md` when done.
</output>
