---
phase: 06-sistema-editorial-seo-geo-e-llm
plan: "08"
type: execute
wave: 4
depends_on: [06-07]
files_modified:
  - app/api/leads/capture/route.ts
  - app/api/notify/route.ts
  - lib/deskcomm-leads.ts
  - lib/supabase-leads.ts
  - supabase/migrations/20260923_editorial_attribution.sql
  - tests/deskcomm-leads.test.ts
  - tests/notify-route.test.ts
  - tests/funnel-database.test.ts
autonomous: true
requirements: [BLOG-06]
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-02/D-03: artigo, cluster e CTA persistem no mesmo leadCaptureId/estágios da Phase 1, prontos para o relatório protegido do plano 06-09."
    - "First content touch permanece imutável; last content touch pode atualizar; ausência histórica não é preenchida retroativamente."
    - "Deskcomm recebe somente campos escalares allowlisted e Supabase mantém projeção de backup; nenhum deles recebe token/segredo."
    - "Nomes/campos editoriais são fundamentados nos contratos atuais de Phase 1, Deskcomm e Supabase; suporte/ativação externa permanece checkpoint, não integração nova."
    - "Todos os testes usam transportes simulados e nunca criam lead, e-mail, reserva ou deploy real."
  artifacts:
    - path: supabase/migrations/20260923_editorial_attribution.sql
      provides: "Persistência/merge editorial first/last no registro comercial existente"
  key_links:
    - from: app/api/leads/capture/route.ts
      to: lib/deskcomm-leads.ts
      via: "payload sanitizado mantém leadCaptureId e attribution escalar"
      pattern: "captureDeskcommLead"
    - from: lib/supabase-leads.ts
      to: supabase/migrations/20260923_editorial_attribution.sql
      via: "record_funnel_lead preserva first e atualiza last no jsonb"
      pattern: "record_funnel_lead"
---

<objective>
Levar a atribuição editorial do payload do navegador até a captura e o backup comercial já existentes.

Purpose: preparar a persistência durável exigida por BLOG-06 além de GA4, preservando Deskcomm-first, identidade e estágios da Phase 1.
Output: validação server-side, projeções Deskcomm/Supabase e migration first/last prontas para o relatório protegido do plano 06-09.
</objective>

<execution_context>
@C:/Users/Igor Marin Santos/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Igor Marin Santos/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-sistema-editorial-seo-geo-e-llm/06-CONTEXT.md
@.planning/phases/06-sistema-editorial-seo-geo-e-llm/06-RESEARCH.md
@.planning/phases/06-sistema-editorial-seo-geo-e-llm/06-PATTERNS.md
@.planning/phases/06-sistema-editorial-seo-geo-e-llm/06-07-SUMMARY.md
@.planning/phases/01-medi-o-comercial-do-funil/01-CONTEXT.md
@.planning/phases/01-medi-o-comercial-do-funil/01-01-SUMMARY.md
@.planning/phases/01-medi-o-comercial-do-funil/01-02-SUMMARY.md
@docs/quality/phase-3-tracking-crm.md

<interfaces>
- `leadCaptureId` é a identidade estável entre captura precoce, confirmação, Deskcomm e Supabase.
- `captureDeskcommLead` descarta objetos aninhados e aceita somente campos escalares allowlisted.
- `record_funnel_lead` já preserva chaves `first_*` em `utm jsonb`; a migration deve estender essa regra, não criar outro lead/funil.
- `read_funnel_report` é o contrato protegido do painel; ampliar sua resposta com breakdown editorial.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Validar e encaminhar atribuição nas duas rotas de lead</name>
  <precondition>Os artefatos Phase 1 `app/api/leads/capture/route.ts`, `app/api/notify/route.ts` e `leadCaptureId` existem e os testes atuais passam sem integração real.</precondition>
  <files>app/api/leads/capture/route.ts, app/api/notify/route.ts, tests/notify-route.test.ts</files>
  <read_first>app/api/leads/capture/route.ts, app/api/notify/route.ts, lib/editorial/analytics.ts, lib/deskcomm-leads.ts, lib/supabase-leads.ts, tests/editorial/analytics-attribution.test.ts, tests/notify-route.test.ts, .planning/phases/01-medi-o-comercial-do-funil/01-CONTEXT.md, docs/quality/phase-3-tracking-crm.md, .planning/phases/06-sistema-editorial-seo-geo-e-llm/06-CONTEXT.md, .planning/phases/06-sistema-editorial-seo-geo-e-llm/06-RESEARCH.md</read_first>
  <behavior>
    - Captura precoce e confirmação aceitam o mesmo shape editorial allowlisted e o mesmo leadCaptureId.
    - Valores desconhecidos, longos, aninhados, PII ou texto livre são descartados/rejeitados sem autorizar operação.
    - `turnstileToken` continua fora de storage/adapters e nenhum retry cria identidade nova.
  </behavior>
  <action>Validar no servidor os campos first/last editoriais projetados por `getUtmLeadPayload`, limitando nomes, tamanho, enum de intent e formatos de slug/cluster/CTA. Encaminhar o mesmo contexto sanitizado nas chamadas de captura precoce e confirmação, mantendo `leadCaptureId`, ordem Deskcomm-first, regras de fit/agenda e remoção do Turnstile. Tratar atribuição como contexto declarativo, nunca como autorização. Escrever testes com mocks locais para payload válido/adversarial e reenvio; não usar endpoint, segredo ou transporte real.</action>
  <acceptance_criteria>
    - Ambas as rotas sanitizam e passam o mesmo contexto editorial escalar.
    - Campos inválidos/PII não alcançam Deskcomm, Supabase, logs ou analytics.
    - Fluxo de agendamento/qualificação da Phase 1 não muda.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- tests/editorial/analytics-attribution.test.ts tests/notify-route.test.ts</automated>
    <fails_when>o comando sai diferente de zero, payload adversarial passa ou captura/confirmação divergem</fails_when>
    <automated>npm run typecheck</automated>
    <fails_when>o comando sai diferente de zero ou o contrato das rotas fica incompatível</fails_when>
  </verify>
  <done>O contexto editorial validado acompanha o mesmo lead nas duas bordas públicas.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persistir first/last editorial no CRM e backup sem reescrever histórico</name>
  <reversibility rating="costly">A migration altera a função consumida pelo funil e o shape escalar enviado ao CRM; desfazer exige migração compensatória e coordenação com relatórios.</reversibility>
  <files>lib/deskcomm-leads.ts, lib/supabase-leads.ts, supabase/migrations/20260923_editorial_attribution.sql, tests/deskcomm-leads.test.ts, tests/funnel-database.test.ts</files>
  <read_first>lib/deskcomm-leads.ts, lib/supabase-leads.ts, supabase/migrations/20260917_funnel_reliability.sql, tests/deskcomm-leads.test.ts, tests/funnel-database.test.ts, lib/editorial/analytics.ts, .planning/phases/01-medi-o-comercial-do-funil/01-CONTEXT.md, docs/quality/phase-3-tracking-crm.md, .planning/phases/06-sistema-editorial-seo-geo-e-llm/06-RESEARCH.md, .planning/phases/06-sistema-editorial-seo-geo-e-llm/06-PATTERNS.md</read_first>
  <behavior>
    - Deskcomm recebe first/last article/cluster/intent/CTA como escalares allowlisted, sem objeto aninhado.
    - Supabase faz merge preservando toda chave first existente e atualizando last válida.
    - Linhas históricas sem atribuição continuam nulas/ausentes; nenhum backfill infere origem.
    - Reenvio idempotente não cria lead adicional.
  </behavior>
  <action>Ampliar a allowlist de `captureDeskcommLead` somente com campos editoriais escalares aprovados e manter secrets/tokens/payload bruto fora. Projetar o mesmo objeto sanitizado para `saveLeadSubmission`. Criar migration aditiva que substitui de forma versionada a função de upsert para preservar first/update last dentro de `utm jsonb`, mantendo RLS, revokes e grant apenas para service_role. Não aplicar a migration em Supabase externo neste plano; provar a ordem e semântica em PGlite, sem inferir histórico.</action>
  <acceptance_criteria>
    - Payload CRM contém apenas escalares editoriais allowlisted e dados comerciais já existentes.
    - Upsert preserva first, atualiza last e permanece idempotente.
    - Migration não concede acesso público nem executa contra produção.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:lead-capture</automated>
    <fails_when>o comando sai diferente de zero ou o adaptador permite objeto/PII/token ou perde idempotência</fails_when>
    <automated>npm test -- tests/funnel-database.test.ts</automated>
    <fails_when>o comando sai diferente de zero, first é sobrescrito, last não atualiza ou permissões ficam públicas</fails_when>
  </verify>
  <done>Atribuição editorial persiste no mesmo registro comercial e na projeção de backup com semântica first/last.</done>
</task>

</tasks>

## Artifacts this phase produces

- `supabase/migrations/20260923_editorial_attribution.sql` — merge first/last no registro comercial existente.
- Contrato server-side de atribuição editorial em captura/confirmação/Deskcomm/Supabase.
- Base persistida e testada que o plano 06-09 agrega no endpoint e painel internos.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| browser → rotas públicas | Atribuição não confiável entra junto a dados do lead |
| rotas → Deskcomm/Supabase | Contexto sanitizado cruza para sistemas de registro |

## STRIDE Threat Register — ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-06-03 | Information Disclosure | rotas/adapters | high | mitigate | validação server-side, allowlist escalar e remoção de token/PII antes dos transportes |
| T-06-07 | Tampering | atribuição enviada pelo browser | medium | mitigate | tratar como contexto não autorizador, validar shape/size e correlacionar pela identidade existente |
</threat_model>

<verification>
- `npm run test:lead-capture`
- `npm test -- tests/editorial/analytics-attribution.test.ts tests/notify-route.test.ts tests/funnel-database.test.ts`
- `npm run typecheck`
- `git diff --check`
</verification>

<success_criteria>
- A atribuição editorial chega ao mesmo registro comercial e preserva first/last sem inferir histórico.
- Deskcomm continua operacional e Supabase continua projeção/coordenação, conforme Phase 1.
- Nenhuma verificação dispara lead, e-mail, reserva, migration remota ou deploy real.
</success_criteria>

<output>
Create `.planning/phases/06-sistema-editorial-seo-geo-e-llm/06-08-SUMMARY.md` when done.
</output>
