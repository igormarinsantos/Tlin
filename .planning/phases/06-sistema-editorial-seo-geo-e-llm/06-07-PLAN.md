---
phase: 06-sistema-editorial-seo-geo-e-llm
plan: "07"
type: execute
wave: 3
depends_on: [06-02]
files_modified:
  - lib/editorial/analytics.ts
  - lib/analytics-events.ts
  - lib/utm.ts
  - components/UTMTracker.tsx
  - components/QualificationController.tsx
  - components/blog/EditorialCta.tsx
  - components/blog/ShareBar.tsx
  - tests/analytics-events.test.ts
  - tests/editorial/analytics-attribution.test.ts
autonomous: true
requirements: [BLOG-06]
estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-02: analytics diferencia descoberta/engajamento/clique de demo confirmada, qualificação humana e venda; clique ou formulário não são resultado final."
    - "D-03: o contexto editorial amplia o contrato de first/last touch da Phase 1 sem criar identidade ou funil paralelo."
    - "Primeiro toque editorial é imutável durante o TTL; último toque atualiza com a navegação editorial válida."
    - "Nenhum evento aceita e-mail, telefone, título/texto livre, query, hash ou outra PII/alta cardinalidade."
  artifacts:
    - path: lib/editorial/analytics.ts
      provides: "EditorialTouch, persistência first/last, validação e projeções para analytics/lead"
    - path: tests/editorial/analytics-attribution.test.ts
      provides: "Contrato browser-side de atribuição sem PII"
  key_links:
    - from: components/UTMTracker.tsx
      to: lib/editorial/analytics.ts
      via: "captura de toque editorial por pathname no observer global existente"
      pattern: "captureEditorialTouch"
    - from: lib/utm.ts
      to: lib/editorial/analytics.ts
      via: "getUtmLeadPayload incorpora first/last editorial sem substituir UTMs"
      pattern: "getEditorialLeadPayload"
---

<objective>
Criar atribuição editorial durável no navegador e eventos controlados até a abertura do funil.

Purpose: iniciar BLOG-06 com first/last content touch, CTA e compartilhamento seguros, sem parar em GA4.
Output: store editorial com TTL, allowlist ampliada, CTAs/compartilhamento instrumentados e payload de lead enriquecido.
</objective>

<execution_context>
@C:/Users/Igor Marin Santos/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Igor Marin Santos/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-sistema-editorial-seo-geo-e-llm/06-CONTEXT.md
@.planning/phases/06-sistema-editorial-seo-geo-e-llm/06-RESEARCH.md
@.planning/phases/06-sistema-editorial-seo-geo-e-llm/06-PATTERNS.md
@.planning/phases/06-sistema-editorial-seo-geo-e-llm/06-02-SUMMARY.md
@.planning/phases/01-medi-o-comercial-do-funil/01-CONTEXT.md
@.planning/phases/01-medi-o-comercial-do-funil/01-01-SUMMARY.md
@.planning/phases/01-medi-o-comercial-do-funil/01-02-SUMMARY.md

<interfaces>
- `captureUtms`, `getUtmLeadPayload` e `trackFunnelEvent` em `lib/utm.ts` são o contrato browser-side da Phase 1.
- `cleanAnalyticsParams` em `lib/analytics-events.ts` usa allowlist explícita e redige padrões de contato.
- `QualificationController` aceita hoje `{ plan?, source? }` em `open-qualification`; ampliar sem criar listener paralelo.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Persistir first e last content touch no tracker global</name>
  <reversibility rating="costly">Os nomes escalares de atribuição passam a circular pelo payload Phase 1; renomeá-los depois exige migração coordenada de browser, rotas, CRM e relatórios.</reversibility>
  <files>lib/editorial/analytics.ts, lib/utm.ts, components/UTMTracker.tsx, tests/editorial/analytics-attribution.test.ts</files>
  <read_first>lib/utm.ts, components/UTMTracker.tsx, lib/analytics-events.ts, lib/editorial/types.ts, lib/editorial/queries.ts, tests/analytics-events.test.ts, tests/editorial/analytics-attribution.test.ts, .planning/phases/01-medi-o-comercial-do-funil/01-CONTEXT.md, .planning/phases/06-sistema-editorial-seo-geo-e-llm/06-CONTEXT.md, .planning/phases/06-sistema-editorial-seo-geo-e-llm/06-RESEARCH.md, .planning/phases/06-sistema-editorial-seo-geo-e-llm/06-PATTERNS.md</read_first>
  <behavior>
    - Primeira visita editorial válida grava first touch uma vez; visitas seguintes atualizam somente last touch.
    - Estado expirado, futuro, malformado ou com slug/cluster/intent fora do registro é descartado.
    - `getUtmLeadPayload` mantém UTMs/click IDs e acrescenta campos escalares `first_*`/`last_*` editoriais.
    - Navegação não editorial não fabrica toque e não apaga um first touch válido.
  </behavior>
  <action>Criar `EditorialTouch` com `article_slug`, `content_cluster`, `content_intent`, `cta_id` opcional e `captured_at`; usar chaves próprias de localStorage/cookie e o mesmo TTL de 30 dias, sem armazenar token, PII, title ou texto. Validar IDs por formatos/taxonomia controlados e URLs por pathname limpo. Integrar a captura ao ciclo existente do `UTMTracker` por mudança de pathname, sem observer global adicional. Exportar projeções separadas para evento e lead; ampliar `getUtmLeadPayload()` com first/last editorial sem reutilizar `utm_campaign`, mantendo D-03 e a semântica first-touch de Phase 1.</action>
  <acceptance_criteria>
    - First touch não muda após nova visita; last touch muda somente para contexto válido.
    - Cookies/storage expirados ou malformados são limpos/ignorados.
    - Payload de lead contém aquisição externa e atribuição editorial em campos distintos.
    - Nenhum token ou PII é persistido.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- tests/editorial/analytics-attribution.test.ts tests/analytics-events.test.ts</automated>
    <fails_when>o comando sai diferente de zero ou first/last, TTL, validação ou ausência de PII falham</fails_when>
    <automated>npm run typecheck</automated>
    <fails_when>o comando sai diferente de zero ou a projeção editorial não compila no tracker/payload existente</fails_when>
  </verify>
  <done>O navegador mantém first/last editorial controlado e o inclui no payload durável do lead.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Instrumentar view, CTA e share com IDs de baixa cardinalidade</name>
  <files>lib/analytics-events.ts, components/QualificationController.tsx, components/blog/EditorialCta.tsx, components/blog/ShareBar.tsx, tests/editorial/analytics-attribution.test.ts</files>
  <read_first>lib/analytics-events.ts, lib/utm.ts, lib/editorial/analytics.ts, components/QualificationController.tsx, components/blog/EditorialCta.tsx, components/blog/ShareBar.tsx, tests/analytics-events.test.ts, tests/editorial/analytics-attribution.test.ts, AGENTS.md, .planning/phases/06-sistema-editorial-seo-geo-e-llm/06-RESEARCH.md, .planning/phases/06-sistema-editorial-seo-geo-e-llm/06-PATTERNS.md</read_first>
  <behavior>
    - Eventos aceitam apenas `article_slug`, `content_cluster`, `content_intent`, `content_group`, `cta_id`, `cta_location`, `method`, `content_type` e `item_id` controlados.
    - CTA emite `article_cta_click`, preserva o contexto no detalhe de `open-qualification` e abre uma única instância global.
    - Share emite o evento recomendado `share` com method/content_type/item_id, sem título ou URL com query.
    - E-mail, telefone, texto livre e chaves não allowlisted são descartados/redigidos.
  </behavior>
  <action>Ampliar a allowlist somente com IDs editoriais controlados e limites de formato/tamanho; não liberar objeto arbitrário. Fazer `EditorialCta` registrar toque/CTA, emitir `article_cta_click` e despachar o detalhe editorial ao listener global. Ampliar o tipo do `QualificationController` para transportar o contexto e manter a trava de abertura única. Instrumentar `ShareBar` com `share`, mapeando somente o método fixo e o slug como `item_id`. Preservar URL sem query/hash e proibição de PII. Testar payloads aceitos e rejeitados.</action>
  <acceptance_criteria>
    - Eventos editoriais chegam ao transportador somente com chaves allowlisted e valores controlados.
    - `open-qualification` recebe article/cluster/intent/CTA sem listener local no blog.
    - Compartilhamento não envia title, e-mail, telefone, termo digitado ou URL sensível.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- tests/editorial/analytics-attribution.test.ts tests/analytics-events.test.ts tests/qualification-controller.test.tsx</automated>
    <fails_when>o comando sai diferente de zero, PII/texto livre passa ou o CTA não abre exatamente um fluxo com contexto</fails_when>
    <automated>npm run typecheck</automated>
    <fails_when>o comando sai diferente de zero por detalhe de evento ou parâmetros incompatíveis</fails_when>
  </verify>
  <done>Views, CTA e shares editoriais são mensuráveis por IDs seguros e o contexto segue para o funil.</done>
</task>

</tasks>

## Artifacts this phase produces

- `lib/editorial/analytics.ts` — `EditorialTouch`, `captureEditorialTouch`, `getEditorialEventPayload`, `getEditorialLeadPayload`.
- Allowlist editorial em `cleanAnalyticsParams`.
- Detalhe tipado de `open-qualification` com atribuição editorial.
- `tests/editorial/analytics-attribution.test.ts` — first/last/TTL/PII/CTA/share.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| URL/conteúdo → storage browser | Identidade editorial vira estado durável first/last |
| browser → GA/event bus | Parâmetros editoriais saem para analytics |
| CTA → qualification controller | Contexto editorial acompanha a abertura do funil |

## STRIDE Threat Register — ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-06-03 | Information Disclosure | analytics/storage | high | mitigate | allowlist, IDs controlados, URL limpa, redaction e testes explícitos de PII/texto livre |
| T-06-07 | Tampering | editorial touch | medium | mitigate | validar shape/TTL/IDs e tratar contexto como atribuição, nunca autorização |
</threat_model>

<verification>
- `npm test -- tests/editorial/analytics-attribution.test.ts tests/analytics-events.test.ts tests/qualification-controller.test.tsx`
- `npm run typecheck`
- `git diff --check`
</verification>

<success_criteria>
- Atribuição editorial first/last existe além do GA4 e entra no payload de lead.
- Eventos usam baixa cardinalidade e nenhuma PII/texto arbitrário.
- Métricas intermediárias permanecem distintas do resultado comercial de D-02.
</success_criteria>

<output>
Create `.planning/phases/06-sistema-editorial-seo-geo-e-llm/06-07-SUMMARY.md` when done.
</output>
