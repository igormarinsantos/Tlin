---
phase: 06-sistema-editorial-seo-geo-e-llm
plan: "09"
type: execute
wave: 5
depends_on: [06-08]
files_modified:
  - supabase/migrations/20260923_editorial_attribution.sql
  - app/api/internal/funnel/route.ts
  - app/internal/funnel/page.tsx
  - tests/funnel-database.test.ts
  - tests/funnel-report-route.test.ts
autonomous: true
requirements: [BLOG-06]
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-02/D-03: o relatório relaciona artigo, cluster e CTA persistidos às mesmas demos confirmadas, qualificações humanas e vendas da Phase 1."
    - "A rota interna continua protegida por token e expõe somente agregados, nunca PII ou linhas individuais."
    - "Ausência histórica de atribuição aparece como grupo não atribuído, sem backfill ou crédito inventado."
    - "Descoberta, clique, demo confirmada, qualificação humana e venda permanecem estágios distintos."
  artifacts:
    - path: supabase/migrations/20260923_editorial_attribution.sql
      provides: "Extensão de read_funnel_report com breakdown editorial agregado"
    - path: app/api/internal/funnel/route.ts
      provides: "Contrato autenticado do breakdown por conteúdo"
    - path: app/internal/funnel/page.tsx
      provides: "Visualização protegida de conteúdo até demo, qualificação e venda"
  key_links:
    - from: app/api/internal/funnel/route.ts
      to: supabase/migrations/20260923_editorial_attribution.sql
      via: "read_funnel_report fornece a coleção content agregada"
      pattern: "read_funnel_report"
    - from: app/internal/funnel/page.tsx
      to: app/api/internal/funnel/route.ts
      via: "a página interna preserva autenticação/período e renderiza o shape agregado"
      pattern: "content"
---

<objective>
Expor a atribuição persistida pelo plano 06-08 no relatório comercial protegido existente.

Purpose: completar BLOG-06 no loop local simulado sem misturar indicadores intermediários com resultado comercial.
Output: agregação de banco, contrato autenticado e painel interno por artigo/cluster/CTA.
</objective>

<execution_context>
@C:/Users/Igor Marin Santos/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Igor Marin Santos/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/06-sistema-editorial-seo-geo-e-llm/06-CONTEXT.md
@.planning/phases/06-sistema-editorial-seo-geo-e-llm/06-RESEARCH.md
@.planning/phases/06-sistema-editorial-seo-geo-e-llm/06-PATTERNS.md
@.planning/phases/06-sistema-editorial-seo-geo-e-llm/06-08-SUMMARY.md
@.planning/phases/01-medi-o-comercial-do-funil/01-CONTEXT.md
@.planning/phases/01-medi-o-comercial-do-funil/01-02-SUMMARY.md
@docs/quality/phase-3-tracking-crm.md
@docs/design-system.md

<interfaces>
- `record_funnel_lead` e o merge first/last editorial do plano 06-08 são a única fonte persistida; este plano não cria outro lead, identidade ou funil.
- `read_funnel_report` é o contrato protegido existente e conserva os marcos Phase 1 de booked, qualified e won.
- `app/api/internal/funnel/route.ts` e `app/internal/funnel/page.tsx` já implementam autenticação/período; ampliar o shape sem criar endpoint público.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Agregar atribuição editorial nos marcos comerciais existentes</name>
  <reversibility rating="costly">A migration substitui a função de relatório consumida pelo painel; desfazer exige migration compensatória mantendo o shape anterior.</reversibility>
  <files>supabase/migrations/20260923_editorial_attribution.sql, tests/funnel-database.test.ts</files>
  <read_first>supabase/migrations/20260923_editorial_attribution.sql, supabase/migrations/20260917_funnel_reliability.sql, tests/funnel-database.test.ts, .planning/phases/06-sistema-editorial-seo-geo-e-llm/06-08-SUMMARY.md, .planning/phases/01-medi-o-comercial-do-funil/01-CONTEXT.md, docs/quality/phase-3-tracking-crm.md</read_first>
  <behavior>
    - `read_funnel_report` retorna coleção `content` por first/last article, cluster e CTA com leads, demos confirmadas, qualificados humanos e vendas.
    - Marcos comerciais usam os mesmos estados booked/qualified/won da Phase 1; evento GA4, clique e lead_score não promovem estágio.
    - Registros sem atribuição formam grupo explícito não atribuído e não recebem origem inferida.
    - A função mantém revokes, execução por service_role e ausência de PII nas linhas agregadas.
  </behavior>
  <action>Escrever primeiro os casos PGlite em `tests/funnel-database.test.ts`, cobrindo atribuído, não atribuído, first/last distintos e cada estágio comercial. Estender de forma versionada `read_funnel_report` na migration já iniciada pelo plano 06-08 para acrescentar a coleção `content`, agregada somente pelos IDs editoriais escalares persistidos. Reutilizar booked, qualified e won existentes per D-02/D-03, preservar período e totais atuais, e manter RLS/revokes/grant apenas para service_role. Não aplicar migration em Supabase externo nem inferir atribuição para linhas históricas.</action>
  <acceptance_criteria>
    - O breakdown usa exclusivamente dados persistidos e os marcos comerciais existentes.
    - Não atribuído é explícito e first/last não são reescritos pelo relatório.
    - A função não retorna nome, telefone, e-mail, payload bruto ou linha individual.
    - Permissões públicas continuam revogadas e nenhum banco externo é chamado.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- tests/funnel-database.test.ts</automated>
    <fails_when>o comando sai diferente de zero, a agregação credita estágio incorreto, omite não atribuído, expõe PII ou amplia permissões</fails_when>
    <automated>git diff --check</automated>
    <fails_when>o comando aponta whitespace inválido na migration ou no teste</fails_when>
  </verify>
  <done>O banco produz breakdown editorial sem alterar a identidade ou a semântica comercial da Phase 1.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Renderizar o breakdown somente no relatório autenticado</name>
  <files>app/api/internal/funnel/route.ts, app/internal/funnel/page.tsx, tests/funnel-report-route.test.ts</files>
  <read_first>app/api/internal/funnel/route.ts, app/internal/funnel/page.tsx, lib/funnel-store.ts, tests/funnel-report-route.test.ts, tests/funnel-database.test.ts, docs/quality/phase-3-tracking-crm.md, docs/design-system.md, .planning/phases/06-sistema-editorial-seo-geo-e-llm/06-CONTEXT.md</read_first>
  <behavior>
    - A API sem token continua 401 e a resposta autenticada acrescenta somente a coleção agregada `content`.
    - A página mostra artigo/cluster/CTA, leads, demos confirmadas, qualificados humanos e vendas, incluindo não atribuído.
    - Cliques e descoberta permanecem indicadores separados e não aparecem como demo, qualificação ou venda.
    - Nenhuma resposta ou célula contém nome, telefone, e-mail ou outro dado individual.
  </behavior>
  <action>Escrever primeiro os testes de autenticação e shape com mocks locais. Ampliar a rota interna para repassar a coleção `content` do mesmo `read_funnel_report`, conservando bearer token, período e erros existentes. Atualizar a página protegida para renderizar os agregados com componentes/tokens atuais, rótulos claros para first/last e não atribuído, e distinção visual entre indicadores de conteúdo e booked/qualified/won. Não criar endpoint público, evento de conversão paralelo ou chamada real ao Supabase.</action>
  <acceptance_criteria>
    - Rota sem token retorna 401; rota autenticada mantém totais existentes e acrescenta o breakdown esperado.
    - O painel distingue descoberta/clique de demo confirmada, qualificação humana e venda.
    - Resposta e UI são agregadas e não expõem PII.
    - Testes usam somente PGlite/mocks e não fazem lead, e-mail, agenda, migration remota ou deploy.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- tests/funnel-report-route.test.ts tests/funnel-database.test.ts</automated>
    <fails_when>o comando sai diferente de zero, autenticação pode ser contornada, PII aparece ou o shape diverge da função agregada</fails_when>
    <automated>npm run typecheck</automated>
    <fails_when>o comando sai diferente de zero ou rota/página não compartilham o contrato do relatório</fails_when>
  </verify>
  <done>A equipe consegue comparar conteúdo por demo confirmada, qualificação humana e venda no relatório protegido.</done>
</task>

</tasks>

## Artifacts this phase produces

- Extensão `content` de `read_funnel_report` na migration editorial.
- Contrato protegido do breakdown em `app/api/internal/funnel/route.ts`.
- Visualização agregada em `app/internal/funnel/page.tsx`.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| banco → relatório interno | Agregados comerciais cruzam uma função service_role e uma rota autenticada |
| rota interna → UI protegida | Dados agregados chegam ao painel sem expor linhas individuais |

## STRIDE Threat Register — ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-06-03 | Information Disclosure | função/rota/painel | high | mitigate | agregação sem PII, contrato de resposta allowlisted e testes de ausência de dados individuais |
| T-06-08 | Elevation of Privilege | relatório interno | high | mitigate | conservar bearer token, revokes/RLS/service_role e testes explícitos de 401 |
| T-06-07 | Tampering | atribuição agregada | medium | mitigate | ler somente IDs persistidos, não autorizar operação e não inferir histórico ausente |
</threat_model>

<verification>
- `npm test -- tests/funnel-database.test.ts tests/funnel-report-route.test.ts`
- `npm run typecheck`
- `git diff --check`
</verification>

<success_criteria>
- BLOG-06 fecha o loop local simulado de artigo/cluster/CTA até demo, qualificação humana e venda.
- D-02/D-03 permanecem no mesmo funil/identidade e métricas intermediárias não viram resultado final.
- Relatório continua autenticado, agregado e livre de PII.
</success_criteria>

<output>
Create `.planning/phases/06-sistema-editorial-seo-geo-e-llm/06-09-SUMMARY.md` when done.
</output>
