---
phase: 01-medi-o-comercial-do-funil
plan: "02"
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - app/api/notify/route.ts
  - app/api/webhooks/deskcomm/route.ts
  - components/LeadQualificationPopup.tsx
  - components/Turnstile.tsx
  - lib/supabase-leads.ts
  - supabase/migrations/20260915_deskcomm_backup_projection.sql
  - tests/lead-funnel-route.test.ts
  - tests/deskcomm-status-webhook.test.ts
files_deleted: []
autonomous: false
requirements: [FUN-01, FUN-02, FUN-03, OTIM-01]
user_setup:
  - service: Deskcomm
    why: "O CRM precisa publicar as mudanças de estágio para a URL segura da landing, que as espelha sem se tornar fonte de verdade."
    dashboard_config:
      - task: "Configurar webhook de saída de estágios comerciais"
        location: "Deskcomm → Automações/Webhooks"
        details: "Apontar para `https://tlin.ia.br/api/webhooks/deskcomm`, usar o segredo configurado e incluir lead_id/contact_id, estágio, horário do evento e chave de correlação da captura."
must_haves:
  truths:
    - "WhatsApp válido cria ou atualiza primeiro o lead operacional Deskcomm, inclusive sem agendamento de demo."
    - "Leads sem fit imediato chegam ao Deskcomm como a desenvolver e não são marcados como perdidos."
    - "Demo agendada, qualificação e venda podem ser relacionados à origem primária e ao último contexto de aquisição sem considerar formulário como venda."
    - "Falha do backup Supabase não impede uma captura Deskcomm bem-sucedida; falha do Deskcomm não retorna sucesso falso."
  artifacts:
    - path: app/api/notify/route.ts
      provides: "Orquestrador Deskcomm-first para os marcos de captura, qualificação e demo"
      exports: [POST]
    - path: app/api/webhooks/deskcomm/route.ts
      provides: "Receptor autenticado de alterações comerciais Deskcomm para espelho de backup"
      exports: [POST]
    - path: supabase/migrations/20260915_deskcomm_backup_projection.sql
      provides: "Campos de correlação e snapshots CRM no backup secundário"
      min_lines: 20
  key_links:
    - from: components/LeadQualificationPopup.tsx
      to: app/api/notify/route.ts
      via: "POST de WhatsApp captado, qualificação concluída e demo selecionada"
      pattern: "fetch\\('/api/notify'"
    - from: app/api/notify/route.ts
      to: lib/deskcomm-leads.ts
      via: "captura e atualização primárias no CRM antes do espelhamento"
      pattern: "capture.*Deskcomm|update.*Deskcomm"
    - from: app/api/webhooks/deskcomm/route.ts
      to: lib/supabase-leads.ts
      via: "espelhamento de evento comercial autenticado sem retorno de comando ao CRM"
      pattern: "save|update"
  prohibitions:
    - statement: "A mudança de estágio recebida do Deskcomm MUST NOT disparar uma nova criação, atualização ou agenda no Deskcomm a partir do Supabase."
      status: unresolved
      verification: null
    - statement: "A landing MUST NOT mostrar uma demo confirmada quando o Deskcomm rejeita a reserva ou quando não há identificador de lead/contacto correlacionado."
      status: unresolved
      verification: null
---

<objective>
Migrar o fluxo da landing para Deskcomm-first e projetar eventos comerciais no Supabase apenas como backup consultável.

Purpose: transformar origem, qualificação, demo e venda em uma cadeia comercial única, sem reestruturar visualmente a LP.
Output: marcos explícitos de captura, webhook seguro de retorno, dados de backup correlacionados e testes de degradação.

Decisions covered: D-01 (estágios comerciais no Deskcomm), D-02 (Supabase apenas espelho), D-03 (contrato reutilizável pelo relatório futuro), D-04 (formulário é SDR digital), D-05 (agenda só para fit aprovado), D-06 (baixo fit vai para a desenvolver), D-08 (abandono após WhatsApp é elegível a nutrição) e D-09 (atribuição first/last touch).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
@~/.claude/gsd-core/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-medi-o-comercial-do-funil/01-CONTEXT.md
@.planning/phases/01-medi-o-comercial-do-funil/01-RESEARCH.md
@.planning/phases/01-medi-o-comercial-do-funil/01-01-PLAN.md
@components/LeadQualificationPopup.tsx
@app/api/notify/route.ts
@lib/supabase-leads.ts
@lib/utm.ts
@supabase/migrations/20260602210412_create_lead_form_submissions.sql
</context>

<threat_model>
- T-04: webhook de saída falsificado pode contaminar dados de vendas; mitigar com segredo, assinatura/tempo de validade quando suportados e rejeição de payload sem identificador CRM.
- T-05: CRM indisponível pode ser escondido por sucesso do backup; mitigar respondendo erro explícito e não exibindo sucesso/demonstrativo de agenda.
- T-06: evento de retorno pode criar um loop CRM → backup → CRM; mitigar com endpoint apenas de projeção, sem chamada de retorno ao Deskcomm.
- T-07: dados pessoais podem vazar para GA4; mitigar enviando apenas IDs/tags comerciais não identificáveis aos eventos de analytics.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Separar os marcos comerciais do formulário</name>
  <files>components/LeadQualificationPopup.tsx, components/Turnstile.tsx, app/api/notify/route.ts, tests/lead-funnel-route.test.ts</files>
  <read_first>components/LeadQualificationPopup.tsx, components/Turnstile.tsx, components/lead-qualification/constants.ts, app/api/notify/route.ts, lib/utm.ts, lib/deskcomm-leads.ts, tests/deskcomm-leads.test.ts</read_first>
  <action>Montar Turnstile invisível desde a abertura do popup e aguardar um token válido antes do primeiro POST comercial; renovar o token em cada marco se ele expirar. Fazer o popup enviar `whatsapp_captado` assim que o telefone passar a validação aplicável, com `leadCaptureId` gerado por `crypto.randomUUID()` e persistido em sessionStorage do formulário. Se a pessoa abandonar depois desse marco, manter o lead Deskcomm em `em_qualificacao` para a automação de nutrição/remarketing do CRM. Na confirmação, enviar `qualificacao_concluida` com `lead_score`, `lead_quality` e status `a_desenvolver` ou `qualificado`; somente lead qualificado leva `demoSlot` para a reserva. Reescrever `POST /api/notify` para validar `whatsapp_captado` (nome, telefone, leadCaptureId, UTM e Turnstile, sem exigir e-mail) e `qualificacao_concluida` (os campos anteriores mais e-mail/score/qualidade), chamando `lib/deskcomm-leads.ts` antes de e-mail e backup. Manter o desenho e strings existentes; não enviar PII a `trackFunnelEvent`. D-04/D-05/D-06/D-08: o SDR digital decide fit, não agenda baixo fit e deixa abandono pós-WhatsApp para o CRM nutrir.</action>
  <verify><automated>npm run test:lead-capture</automated><fails_when>exit não zero, ou casos de WhatsApp sem demo, baixo fit e demo elegível não forem aprovados</fails_when></verify>
  <acceptance_criteria>
    - WhatsApp válido sem demo chama Deskcomm com status `captado` ou `em_qualificacao`
    - `whatsapp_captado` exige Turnstile válido mas não exige e-mail que só é informado depois no wizard
    - qualificação de baixo fit atualiza Deskcomm para `a_desenvolver` sem tentar `crm_book_appointment`
    - demo só é confirmada na UI quando o Deskcomm retorna reserva bem-sucedida para o lead/contacto correlacionado
    - `POST /api/notify` não usa `supabaseSaved` para definir sucesso comercial
  </acceptance_criteria>
  <done>O fluxo de SDR digital gera e atualiza o mesmo lead Deskcomm nos marcos corretos.</done>
</task>

<task type="auto">
  <name>Task 2: Projetar backup secundário e retorno de estágios Deskcomm</name>
  <files>lib/supabase-leads.ts, supabase/migrations/20260915_deskcomm_backup_projection.sql, app/api/webhooks/deskcomm/route.ts, tests/deskcomm-status-webhook.test.ts</files>
  <read_first>lib/supabase-leads.ts, supabase/migrations/20260602210412_create_lead_form_submissions.sql, app/api/notify/route.ts, lib/rate-limit.ts, .env.local.example</read_first>
  <action>Criar migration aditiva para `deskcomm_lead_id`, `deskcomm_contact_id`, `crm_status`, `crm_status_at`, `lead_capture_id` e um registro de eventos de estágio deduplicado pelo ID externo do Deskcomm. Atualizar o espelho Supabase para receber esses identificadores depois da tentativa CRM, sem lançar exceção que altere a resposta. Criar `POST /api/webhooks/deskcomm` autenticado por `DESKCOMM_STATUS_WEBHOOK_SECRET`, com limite de tamanho, validação de timestamp/ID/status e deduplicação; ele só grava a projeção no Supabase e retorna 2xx/4xx, nunca chama Deskcomm ou agenda uma demo. D-01/D-02/D-03/D-09: os snapshots conservam estágios e origem para consumo futuro sem criar uma medição paralela ao CRM.</action>
  <verify><automated>npm run test:lead-capture</automated><fails_when>exit não zero, ou teste de webhook aceita segredo inválido, duplica evento, ou realiza chamada Deskcomm a partir do receptor</fails_when></verify>
  <acceptance_criteria>
    - migration é aditiva e preserva lead_form_submissions existentes
    - app/api/webhooks/deskcomm/route.ts rejeita segredo inválido e payload sem identificador externo
    - um evento comercial duplicado não cria segundo snapshot
    - falha de Supabase é registrada sem impedir o sucesso já confirmado pelo Deskcomm
  </acceptance_criteria>
  <done>O backup mantém a correlação de origem e estágios recebidos, sem governar o CRM.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Funil comercial Deskcomm-first e espelho secundário implementados.</what-built>
  <how-to-verify>Na LP, faça três envios de teste: (1) WhatsApp válido sem finalizar agenda, (2) baixo fit, (3) fit aprovado com demo. No Deskcomm, confirme o mesmo lead e estágio esperado. Depois altere esse lead para uma etapa comercial e confirme que o backup recebe o evento sem criar novo lead ou nova agenda.</how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

## Artifacts this phase produces

- `app/api/webhooks/deskcomm/route.ts` — authenticated `POST` endpoint that projects Deskcomm status events into backup storage only.
- `supabase/migrations/20260915_deskcomm_backup_projection.sql` — correlation fields and deduplicated CRM-event projection.
- `tests/lead-funnel-route.test.ts` and `tests/deskcomm-status-webhook.test.ts` — behavior tests for Deskcomm-first capture and non-recursive CRM event ingestion.

<verification>
- [ ] `npm run test:lead-capture` conclui com exit 0
- [ ] `npx tsc --noEmit && npm run lint && npm run build` conclui com exit 0
- [ ] As três verificações manuais no Deskcomm foram aprovadas
</verification>

<success_criteria>
- A equipe pode identificar no CRM lead captado, qualificado, demo e resultado comercial associados à origem.
- Supabase contém uma projeção recuperável, mas não conduz nenhuma ação comercial.
- Falhas de CRM e de backup são distinguíveis e não geram falso sucesso.
</success_criteria>

<output>
After completion, create `.planning/phases/01-medi-o-comercial-do-funil/01-02-SUMMARY.md`
</output>
